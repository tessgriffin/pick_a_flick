<div class="container">
  <h2>Create A Group!</h2>
  <%= form_for(@group) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit "Submit" %>

  <% end %>
</div>
