<div class="container">
  <h4>Create a Group!</h4>
  <%= form_for(@group) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit "Submit", class: "btn blue-grey" %>

  <% end %>
</div>
